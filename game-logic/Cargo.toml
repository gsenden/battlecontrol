[package]
name = "game-logic"
version = "0.1.0"
edition = "2024"

[dependencies]
enum_dispatch = "0.3"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

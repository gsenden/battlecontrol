{
  "name": "battlecontrol",
  "private": true,
  "version": "0.1.6",
  "type": "module",
  "scripts": {
    "build:wasm": "wasm-pack build game-logic-wasm --target web --out-dir ../pkg",
    "dev": "npm run build:wasm && cd frontend && npm run dev",
    "build": "npm run build:wasm && cd frontend && npm run build",
    "preview": "cd frontend && npm run preview",
    "test": "cd frontend && npm run test",
    "test:watch": "cd frontend && npm run test:watch",
    "postinstall": "npm run build:wasm && cd frontend && npm install"
  }
}
